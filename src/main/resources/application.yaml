server:
  port: 8080
  servlet:
    context-path: /smart-api

provider:
  host: https://localhost:9443 #Change the host

client:
  client-id: iF94Ra_Z7OSD399Fd6sRCK4u3dwa #Change client-id
  client-secret: X7cF41qfOQ3XZooIiFo4GuXilJQa # Change client-secret
  scope: openid
  authorization-grant-type: authorization_code


spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    data-username: sa
    data-password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  security:
    filter:
      order: 3
    oauth2:
      client:
        registration:
          wso2:
            client-name: WSO2 Identity Server
            client-id: ${client.client-id} #Change client-id
            client-secret: ${client.client-secret} # Change client-secret
            authorization-grant-type: ${client.authorization-grant-type}
            scope: ${client.scope}
        provider:
          wso2:
            issuer-uri: ${provider.host}/oauth2/token

logging:
  level:
    org.springframework.security: DEBUG